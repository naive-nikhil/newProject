<!-- Features Section (Tailwind + small custom CSS) -->
<section
  class="min-h-screen py-20 bg-gradient-to-b from-[#07040b] via-[#10071a] to-[#0b0720]"
>
  <div class="max-w-7xl mx-auto px-6">
    <h2 class="text-4xl md:text-5xl text-white font-light mb-12">Features</h2>

    <div class="grid gap-y-10 md:gap-y-14">
      <!-- Feature item template - copy/modify for each feature -->
      <article class="md:grid md:grid-cols-2 md:items-start gap-8">
        <!-- icon -->
        <div class="relative flex items-center justify-center">
          <div
            class="icon-card w-28 h-28 md:w-32 md:h-32 rounded-2xl flex items-center justify-center"
          >
            <img
              src="https://www.stableapp.cloud/app/uploads/2025/07/icon-recommandations-for-cost-2x.png.webp"
              alt="icon"
              class="w-12 h-12 md:w-16 md:h-16 object-contain"
            />
          </div>
        </div>

        <!-- text -->
        <div class="text-left">
          <h3 class="text-2xl text-white/90 font-semibold mb-3">
            Prioritized recommendations for cost optimization
          </h3>
          <p
            class="text-sm md:text-base text-white/60 leading-relaxed max-w-prose"
          >
            Stable categorizes fixes into Easy, Medium and Hard, along with the
            cold hard numbers of the dollars you can save.
          </p>
        </div>
      </article>

      <!-- Alternate layout example (text left, icon right) -->
      <article
        class="md:grid md:grid-cols-2 md:items-start gap-8 md:flex-row-reverse"
      >
        <div class="relative flex items-center justify-center">
          <div
            class="icon-card w-28 h-28 md:w-32 md:h-32 rounded-2xl flex items-center justify-center"
          >
            <!-- lightning icon placeholder -->
            <svg
              width="44"
              height="44"
              viewBox="0 0 24 24"
              class="fill-current text-purple-200/90"
            >
              <path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"></path>
            </svg>
          </div>
        </div>

        <div class="text-left">
          <h3 class="text-2xl text-white/90 font-semibold mb-3">
            Impact assessment
          </h3>
          <p
            class="text-sm md:text-base text-white/60 leading-relaxed max-w-prose"
          >
            Stable will give you the details on the impact of a change you'll
            make to reduce a service- or resource-specific cost: the length of
            downtime, replacement duration, in-place changes, etc.
          </p>
        </div>
      </article>

      <!-- Cost exploration (left) -->
      <article class="md:grid md:grid-cols-2 md:items-start gap-8">
        <div class="relative flex items-center justify-center">
          <div
            class="icon-card w-28 h-28 md:w-32 md:h-32 rounded-2xl flex items-center justify-center"
          >
            <svg
              width="44"
              height="44"
              viewBox="0 0 24 24"
              class="fill-current text-purple-200/70"
            >
              <path
                d="M10 18a8 8 0 1 1 0-16 8 8 0 0 1 0 16zm7-2l5 5-1.5 1.5L15.5 17.5 17 16z"
              ></path>
            </svg>
          </div>
        </div>

        <div class="text-left">
          <h3 class="text-2xl text-white/90 font-semibold mb-3">
            Cost exploration
          </h3>
          <p
            class="text-sm md:text-base text-white/60 leading-relaxed max-w-prose"
          >
            Stable breaks spend down to reads, writes, storage, and transfer for
            each resource. No more guessing which lever to really pull.
          </p>
        </div>
      </article>

      <!-- Coming Soon / Real-time alerts (right) -->
      <article
        class="md:grid md:grid-cols-2 md:items-start gap-8 md:flex-row-reverse"
      >
        <div class="relative flex items-center justify-center">
          <div
            class="icon-card w-28 h-28 md:w-32 md:h-32 rounded-2xl flex items-center justify-center"
          >
            <!-- small notification bubble -->
            <div class="absolute -top-2 -right-3">
              <span class="badge px-3 py-1 rounded-full text-xs font-medium"
                >Coming Soon</span
              >
            </div>
            <div class="notif-dot absolute -top-3 -left-1">2</div>

            <!-- cloud icon -->
            <svg
              width="44"
              height="44"
              viewBox="0 0 24 24"
              class="fill-current text-purple-200/90"
            >
              <path d="M19 18H6a4 4 0 01.2-8 5 5 0 019.8 0 3 3 0 01.2 6z"
              ></path>
            </svg>
          </div>
        </div>

        <div class="text-left">
          <h3 class="text-2xl text-white/90 font-semibold mb-3">
            Real-time, smart alerts
          </h3>
          <p
            class="text-sm md:text-base text-white/60 leading-relaxed max-w-prose"
          >
            Stable constantly monitors your AWS estate 24/7 and pings you the
            instant a rogue/botched deployment or usage spike threatens your
            budget.
          </p>
        </div>
      </article>

      <!-- Add more features by copying one article block above -->
    </div>
  </div>
</section>

<!-- Custom CSS (put inside your global CSS file or inside a <style> block) -->
<style>
  /* icon-card glassy appearance + soft purple glow */
  .icon-card {
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.02),
      rgba(255, 255, 255, 0.01)
    );
    border: 1px solid rgba(255, 255, 255, 0.04);
    box-shadow:
      0 10px 30px rgba(16, 6, 30, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(6px);
  }

  /* Coming Soon badge */
  .badge {
    background: linear-gradient(
      90deg,
      rgba(193, 129, 238, 0.18),
      rgba(150, 97, 230, 0.22)
    );
    color: #efe6ff;
    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: 0 6px 20px rgba(106, 43, 154, 0.18);
    backdrop-filter: blur(6px);
  }

  /* small notification circle */
  .notif-dot {
    background: linear-gradient(180deg, #7b4dd6, #b88ef8);
    color: white;
    font-size: 11px;
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    box-shadow: 0 6px 18px rgba(123, 77, 214, 0.25);
    border: 1px solid rgba(255, 255, 255, 0.06);
    font-weight: 600;
  }

  /* responsive tweaks */
  @media (min-width: 768px) {
    .icon-card {
      transform: translateY(4px);
    }
  }

  /* subtle appear animation (optional) */
  article {
    opacity: 0;
    transform: translateY(8px);
    animation: fadeUp 600ms ease forwards;
  }
  article:nth-child(1) {
    animation-delay: 120ms;
  }
  article:nth-child(2) {
    animation-delay: 200ms;
  }
  article:nth-child(3) {
    animation-delay: 280ms;
  }
  article:nth-child(4) {
    animation-delay: 360ms;
  }
  @keyframes fadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
