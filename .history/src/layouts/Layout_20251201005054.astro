---
import "../styles/global.css";
// NOTE: Since we are using GSAP for scrolling, we do not need to import any Locomotive Scroll files/components.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

        <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap"
      rel="stylesheet"
    />
    
        <style>
      .logo-hidden {
        opacity: 0;
        transform: scale(0);
      }
    </style>

    <title>Technovel</title>
  </head>
  <body>
      
     
    
    <slot />

    
    <script type="module" defer>
      import gsap from "https://cdn.jsdelivr.net/npm/gsap@3.12.5/index.js";
      import { ScrollTrigger } from "https://cdn.jsdelivr.net/npm/gsap@3.12.5/ScrollTrigger.min.js";

      gsap.registerPlugin(ScrollTrigger);
      
      // 🟢 1. ENABLE GSAP SMOOTH SCROLLING 🟢
      // This replaces Locomotive Scroll and applies smoothing globally.
      ScrollTrigger.normalizeScroll(true);

      document.addEventListener("astro:page-load", () => {
          // Ensure GSAP uses the current body scroll after a page change
          ScrollTrigger.refresh();
          
          // 🟢 2. LOGO ANIMATION (ScrollTrigger will now work normally) 🟢
          const logo = document.querySelector(".logo");
          gsap.fromTo(
            logo,
            { scale: 0, opacity: 0 },
            {
              scale: 1,
              opacity: 1,
              duration: 0.5,
              scrollTrigger: {
                trigger: document.body,
                start: "top -=10", 
                toggleActions: "play none none none",
              },
            }
          );
      });
    </script>

  </body>
</html>

---

## 🎨 Styling Notes

Since you are no longer using the `[data-scroll-container]` element, you should update your styling block.

```astro
<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
  /* ❌ REMOVE THIS RULE as it's no longer needed:
  [data-scroll-container] {
    will-change: transform;
  }
  */
</style>