---
import mouseIcon from "../assets/mouse.png";
---

<section id="about" class="rounded-b-4xl px-4 md:px-8 lg:px-12 py-12 md:py-20 bg-linear-to-b from-white/50 to-30% to-white z-2 relative">
  <div class="max-w-4xl mx-auto flex flex-col items-start">
    
    <h2 class="text-3xl md:text-5xl lg:text-6xl font-bold text-[#006cb7] mb-8">
      About Technovel
    </h2>

    <div class="text-xl md:text-2xl text-black/70 font-light leading-relaxed">
      <p class="mb-6">We don't just build websites and apps; we engineer digital experiences.</p>
      
      <p>
        In simple words, we turn 
        <span class="font-semibold text-[#f38023]">dreams</span> 
        into a digital reality by 
        <span class="font-semibold text-[#f38023]">giving</span> 
        them 
        <span class="font-semibold text-[#f38023]">wings</span>.
      </p>
    </div>

    <div id="expandable-content" class="grid grid-rows-[0fr] transition-all duration-700 ease-in-out w-full">
      <div class="overflow-hidden">
        <div class="text-lg md:text-xl text-black/60 pt-6 space-y-6 leading-relaxed">
          <p>
            Founded on the belief that technology should be accessible and transformative, Technovel bridges the gap between complex code and intuitive design. Whether you are a startup looking for your first identity or an enterprise seeking optimization, our approach is tailored to fit your specific needs.
          </p>
          <p>
            We specialize in creating robust, scalable platforms that grow with you. From pixel-perfect front-end designs to powerful back-end logic, our team ensures that every line of code serves a purpose: pushing your business forward.
          </p>
        </div>
      </div>
    </div>

    <button 
      id="read-more-btn"
      class="mt-6 flex items-center gap-2 text-[#006cb7] font-bold text-lg hover:text-[#f38023] transition-colors duration-300 cursor-pointer group"
    >
      <span id="btn-text">Read More</span>
      <svg id="btn-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 transition-transform duration-300 group-hover:translate-y-1">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
      </svg>
    </button>

    <div class="w-full mt-24 md:mt-32 opacity-60">
      <img src={mouseIcon.src} alt="Scroll Down" class="w-8 h-8 md:w-10 md:h-10 mx-auto brightness-0 mouse-animate" />
      <p class="text-black/50 text-sm md:text-base text-center mt-3 font-medium tracking-wide">Scroll to explore</p>
    </div>

  </div>
</section>

<style>
  /* Mouse Animation */
  .mouse-animate {
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(10px);
    }
    60% {
      transform: translateY(5px);
    }
  }
</style>

<script>
  // Simple Vanilla JS to handle the toggle
  const btn = document.getElementById('read-more-btn');
  const content = document.getElementById('expandable-content');
  const btnText = document.getElementById('btn-text');
  const btnIcon = document.getElementById('btn-icon');
  
  let isExpanded = false;

  if(btn && content) {
    btn.addEventListener('click', () => {
      isExpanded = !isExpanded;
      
      if (isExpanded) {
        // Expand
        content.classList.remove('grid-rows-[0fr]');
        content.classList.add('grid-rows-[1fr]');
        btnText.innerText = "Read Less";
        btnIcon.style.transform = "rotate(180deg)";
      } else {
        // Collapse
        content.classList.remove('grid-rows-[1fr]');
        content.classList.add('grid-rows-[0fr]');
        btnText.innerText = "Read More";
        btnIcon.style.transform = "rotate(0deg)";
        
        // Optional: Scroll back up slightly if the user is deep down
        // document.getElementById('about').scrollIntoView({ behavior: 'smooth' });
      }
    });
  }
</script>